# V2 Campaign Queues

**Maintained by the orchestrator; updated every cycle (heartbeat in `V2_CAMPAIGN_LOG.md`).**
**Priority order:** critical regression/security/reliability → broken harness/gate → dependency blockers → current milestone completion → review findings → next implementation → docs/release cleanup → final audit.
**Rules:** regressions outrank features; no implementation leaves review unresolved; "queue empty" must be proven; evidence beats claims.

Last full-rebuild: cycle 10 close (HEAD f1b07f1).

### Cycle-26 queue deltas (external session: renderer honesty, polling recovery, documentation currentness)

- **Session constraint, stated first: no shell, no tests, no Electron.** This session had file read/write only (shell execution denied; subagent delegation returned no results), so **every change below is code-reviewed, not gate-verified** — no smoke was executed, and nothing here may be recorded as verified until a quiet-tree `rc:validate` run proves it. Each change is listed with the invariant it is intended to hold.
- **Update — cycle-26b (same session, after a capability preflight): execution is available.** A preflight proved shell/node/git/Electron execution through delegated agents and the whole change set below was then gate-verified: `rc:validate` 287/288 with the single failure being the mobile lane's `permission-matrix:smoke` registration gap (fixed in cycle-26b), `instances:service-url:smoke` PASS in-gate, `docker-smoke` and `jobs-version-skew-smoke` PASS, and `qa:acceptance` PASS (9 screenshots, zero renderer errors). The earlier "not executed" caveats in this section are superseded by cycle-26b's evidence; the changes themselves were unchanged.
- **REGRESSION queue — two cycle-20 follow-ups CLOSED in code (tests not run).**
  - **Docker polling gate (cycle-20 follow-up).** `startDockerPagePolling` skipped `refreshDockerStatus()` while the workspace was not ready, so a transient failure (daemon restart, Agent briefly unreachable, node switch) left the Docker page stuck on its error state until a manual Refresh click. The gate is now `document.hidden` only; repeat refreshes stay bounded by the existing in-flight guard, node-context guard, request timeout, and bounded Agent-polling backoff. `scripts/docker-smoke.js` had pinned the old gate string as a regression needle; it now extracts the polling function body and asserts it calls `refreshDockerStatus()` and does **not** gate on readiness.
  - **Durable-jobs panel honesty (cycle-20 follow-up).** `instances:jobs:list` always reads the desktop's own job store while the panel labelled results "for {selected node}". The renderer now refuses to present local jobs under a remote/local-agent node's name: `isJobsScopeApplicationHost()` gates the load, and the panel shows "This panel lists the job history of this computer only…" with a "This computer only" pill. `scripts/jobs-version-skew-smoke.js` gained a leg that drives the real `renderDurableJobs` with `scopeUnavailable: true` and asserts the scope text renders while "No durable jobs recorded" does not. The preload/IPC comments that described the aspirational contract now describe the real one. **Not executed.**
- **SECURITY queue — renderer href hardening (UNPROVEN risk recorded at cycle 15, never routed to a lane).** `getInstanceServiceUrl` rendered `instance.connectionHost` (node-supplied, length-capped, never host-validated) into an `http://` href. It now validates the host as a bare host (IPv4 / bracketed IPv6 / DNS name with underscore tolerance, ≤253 chars) and returns `""` — the link is hidden — on anything carrying URL structure, instead of retargeting the link. New behavioral smoke `scripts/instance-service-url-smoke.js` extracts the real functions and drives 8 legs including 8 injection-shaped hosts; registered as `instances:service-url:smoke` and added to the feature + release tiers. **Not executed.**
- **REGRESSION queue — F4 CLOSED.** The nav badge capped at a bare `99` while the panel counted 111 unread (two numbers for one thing). Past 99 the badge now reads `99+`; the exact count stays in the aria-label.
- **RENDERER queue — copy/dead-end fixes (no behavior change).** Create Server wizard: the node step no longer references a target selector that does not exist in that window, and the "Select a connected Agent node" validation now says where to select one; the wizard footer no longer says "Templates create instances through the existing Debian agent API"; Settings no longer says "handled in Maintenance through trusted desktop IPC" or "enforced by protected IPC paths"; console CPU/RAM placeholders say "Select a server" and Network says "Not reported" (instance network throughput is not reported by the Agent) instead of "Coming soon".
- **DOCS/STATUS queue — currentness pass (cycle-25 truth).** `docs/KNOWN_LIMITATIONS.md` V2 section rewritten from "current as of cycle 9" to cycle 25: the workload-transfer decline deletion bug is fixed (guard + smoke pin), workload transfer and network inventory have renderer surfaces, Y2 is closed, publisher trust + catalog transfer shipped (with honest limits kept), and the still-true limits (Docker rollback, runtime pins, backup destinations, browser surface, job expiry, live Debian install) are kept in substance. `docs/MASTER_ROADMAP.md`: V2-D/E/I/J status lines and annotations corrected where they still said ABSENT/not shipped for shipped surfaces (publisher trust, catalog transfer, adapter contract, audit-export IPC, provenance IPC, alert runtime evidence). `docs/README.md` version metadata 1.9/199 → 2.0/203. `docs/NEW_USER_GUIDE.md` gained **Updating AnxOS** and **Uninstalling AnxOS** sections and a precise remote-pairing step. Smoke header comments citing the old KNOWN_LIMITATIONS/roadmap sentences updated. Live queue tables G1/G2/G4/G6/G7/G8/G9/S1/Y1/Y2 synced to their own delta records.
- **Evidence note:** the published release `v1.9-build203-rc2` was re-verified read-only against the GitHub API — all four artifacts plus `latest.yml`/`latest-linux.yml`/`SHA256SUMS`/`update-manifest.json` present; `website/config.js` and `release-notes.json` metadata match.


### Cycle-26b queue deltas (first-run defects found and closed; gate turned green)

- **ACCEPTANCE queue — FRESH-INSTALL ONBOARDING IS NOW VERIFIED (was NOT VERIFIED).** New harness `scripts/onboarding-fresh-install-smoke.js` (`npm run qa:onboarding`): launches the real Electron app on an isolated profile and drives the whole first-run flow — welcome visible, "Set Up AnxOS", the 7 wizard steps in order (progress text, title, non-empty body for each step), footer Finish, completion toast, `onboarding.completed=true` in localStorage and the config-dir `preferences.json`, dashboard reachable, zero renderer/page errors. **39 checks, 3 consecutive PASS runs**, screenshots per step under `artifacts/onboarding/`.
- **P1 real defect (found by the new harness): the first-run welcome and the entire wizard were unreachable on a fresh install.** `shouldRequireAccountBeforeOnboarding()` treated the *bundled* account config as "account required" — `accountState.configured` means the account service exists (`website/account-config.js` ships in the app per `package.json` build.files), not that the user opted in — so `shouldBlockOnboardingForAccount()` kept `[data-onboarding-welcome]` hidden for every signed-out install, contradicting `docs/NEW_USER_GUIDE.md`, `docs/ONBOARDING_VALIDATION.md`, the Install Guide, the Tester Guide, and the wizard's own "Optional" sign-in copy. Fixed: the gate now waits only for account restoration to resolve (`isAccountRestorationPending()`), preserving the ordering guarantee pinned by `bootstrap:auth-order:smoke` (all four of its gate assertions still pass). `onboarding:smoke` and `account-system:smoke` pass.
- **P1 real defect (same class as the cycle-21 empty-body bug, second instance): onboarding steps 3 and 4 threw on fresh state.** `getLocalAgentOnboardingStatus()` (app.js:38092-38111) passed the null overview target to `isAgentTargetRunning()`, whose `= {}` default covered `undefined` but not `null`; the throw was swallowed by the wizard action handler, leaving "Prepare Node" with an empty body, "Connect Local Agent" without its status/pairing UI, and a raw `Cannot read properties of null (reading 'running')` toast. Fixed at the predicate (`if (!target) return false;`). The harness now walks both steps clean and their screenshots show full bodies.
- **VISUAL queue (sweep of the cycle-25 acceptance screenshots).** Instances: the "Starting / Stopping" card rendered "0 / 0" as three stacked lines because `.instances-summary-card span:not(.instances-summary-card__icon)` also matched the two value spans nested inside `<strong>`; the label rule is now scoped to direct children (`.instances-summary-card > div > span`), verified in a fresh acceptance screenshot. Instances lede no longer says "managed by the Debian agent" (wrong on a Windows host and internal jargon). Public Access recovery copy no longer tells users to use a CLI.
- **GATE — `permission-matrix:smoke` turned green.** It failed because the mobile-pairing lane's new `rest-mobile-claim-page` family (`GET /pair`) was not registered in the harness: the route is served pre-auth before the bearer gate (`agent/src/server.js:479`, rate-limited, documented as pre-auth by design), but the harness's pre-auth path set and its `/api/v1` coverage rule did not know it. Fixed by importing the route's own `CLAIM_PAGE_PATH` constant into the pre-auth set and allowing named browser page routes outside `/api/v1` (fail-closed: a new non-API route must be named there). Result: **59 desktop families (305 channels), 37 Agent REST families — PASS.**
- **F3 (alerts "0 active" vs node health "4 issues") — answered, no change.** The alert engine intentionally covers four conditions (node outages, disk pressure, stale backups, degraded instances) versus the health engine's wider categories, and the notifications page already states that scope in its panel copy. Not a defect; no action.
- **FINAL VERIFICATION WAVE (quiet tree, no edits afterwards).** `rc:validate` **288/288 PASS** (exit 0, ~5m52s; both previously failing suites green — `instances:service-url:smoke` and `permission-matrix:smoke`); `qa:acceptance` **PASS** (9 screenshots, zero renderer errors/warnings); `qa:onboarding` **PASS** (39 checks). Working tree unchanged by the runs (48 porcelain lines before and after); `git diff --check` exit 0 (only pre-existing LF→CRLF working-copy warnings). Logs: `artifacts/preflight/rc-validate-final.log`, `qa-acceptance-final.log`, `qa-onboarding-final.log`.
- **Verification limits.** All runs are on this machine/profile; no cross-machine or CI evidence is claimed. No commits were made — the tree carries a completed-but-uncommitted mobile/responsive lane whose work must not be swept into this session's checkpoint. Rollback anchor for the pre-session tree: dangling commit `3c149c42c023a71df34221bf5c6663bfccd11175` (`git stash create`; no ref moved), retrievable with `git stash apply <hash>` or `git diff <hash>`.


### Cycle-26c queue deltas (worktree untangled, security triaged, safe defaults, recovery drilled)

- **WORKTREE — untangled into local checkpoint commits (no push).** The 67-entry dirty tree was inventoried by lane, every non-mine lane was reviewed (mobile/responsive/remote-update/SSH/FIVEM; no P0), and per-hunk staging produced 11 lane-coherent commits: FIVEM, agent-update, mobile, renderer-honesty, onboarding, nav, security, recovery, responsive, gate-registration, and the qrcode lockfile completion. Each commit was verified (staged file list, `git diff --cached --check`, committed-blob `node --check`/JSON parse) and all working-tree files stayed byte-identical throughout. Pre-split capsule: dangling `e008f9d8…`. The only remaining uncommitted change is a separate session's temporary toast-diagnostic probe in `app.js` (`logToastDiagnostic`, deliberately never committed).
- **SECURITY — the sealed 1012-finding scan was triaged to a defensible verdict.** Line-level verification of every product-code finding in the six classes: 0 TRUE POSITIVE, 1 LIKELY TP (low; SHA-1 fallback in marketplace import), 696 FALSE POSITIVE (435 the sandboxed-renderer `showToast` heuristic), 315 dev-tooling in non-shipped `scripts/**`/`website/site.js`. `npm audit` prod tree reduced to **0 advisories** (js-yaml → 4.3.2; dompurify override → 3.4.16); 5 remaining are dev-only electron-builder chain. Confirmed fixes: Host `%`-smuggling fail-open closed; installer path containment (archive/extractDir/additionalArchives, both builders); remote-agent downgrade guard.
- **NETWORK DEFAULTS — standalone Agent binds `127.0.0.1` by default** (`agent/src/config.js`); wildcard is an explicit `AGENT_HOST` opt-in with a loud startup diagnostic naming the concrete-address alternative (which also gains the strict Host allowlist). Remote management unchanged; desktop-spawned path unchanged; docs updated; `agent:host-trust:smoke` leg E pins it.
- **DNS REBINDING — reproducibly pinned.** New `agent:rebinding:smoke` (`scripts/agent-rebinding-harness.js`) spawns real Agents and pins 419 assertions: concrete bind refuses rebinding-shaped Hosts 421 pre-auth with no echo; wildcard bind pins the accepted gap and its mitigations (zero ACAO, no hostile host in the pairing payload, cross-origin state changes 403); no WebSocket surface; Host edge cases fail closed.
- **RECOVERY — restore drill exists and passes.** New `restore-drill:smoke` performs a real create→backup→destroy→restore→verify cycle with fail-closed legs (truncated/missing archive). Orphan archives are quarantined instead of deleted; device-identity migration verifies its recovery point before its irreversible rewrite; owner-workspace repair takes a verified pre-repair copy. A 20-site migration audit is recorded (4 HIGH safeguarded; MEDIUMs documented).
- **PACKAGING GATE — fixed a real Windows gap.** `packaging-artifact-smoke.js` compared asar entries with the wrong separator (asar returns `\main.js` on Windows) and scanned dependencies comment-blind, so `packaging:smoke`/`artifacts:validate --platform=win` had never passed on Windows and `packaging:smoke` silently skipped inside `rc:validate`. Both fixed with mutation-proven regression legs; it now defaults to the host platform, so the local Windows candidate is genuinely gated.
- **UX — adversarial first-user pass (23 findings, 8 fixed and visually re-verified).** Fixed: create-server no-template dead end; node badge "Healthy" vs "Warning" contradiction (now derives from the shared health model); Agent Control pill stuck "Loading" on the unavailable path; Public Access "1 active" vs "Stopped" contradiction (now "1 configured"); Create Server summary "Connected/Ready" vs node-step refusal (now "Agent node required"); developer-specific Add Node placeholders; node-card name/subtitle concatenation; enabled no-op bulk buttons. Reported-not-fixed: the Firewall raw-agent-error string (smoke-pinned), Fleet-row title spacing, the "Console vs Monitoring" naming decision, and raw `java-app`/`java-runtime` identifiers (need mappings).
- **SECOND MACHINE — one-command wrapper.** New `qa:second-machine` (`scripts/second-machine-acceptance.js`) runs acceptance+onboarding+responsive, copies artifacts, writes `summary.json`, and exits non-zero on failure; documented in `docs/SECOND_MACHINE_ACCEPTANCE.md`. Requires Node 22.x + `npm ci` + a GUI session.
- **PACKAGED CANDIDATE — built and accepted locally (unsigned).** Setup `AnxOS-Control-Center-Setup-2.0-build203.exe` (SHA-256 `5FC0546D…`), portable (`4A1250DA…`), win-unpacked. Packaged `qa:acceptance` PASS (11/11, 9 screenshots, 0 renderer errors). Install/upgrade/reboot/repair/uninstall remain **AJ ACTION REQUIRED: reply exactly `confirmed`** after a signed RC report.
- **FINAL VERIFICATION (cycle-26c).** `rc:validate` **291/291 PASS** (exit 0; `packaging:smoke` now validates the local win candidate in-gate, 8.9s); `qa:acceptance` **PASS** (19 screenshots, 0 renderer errors/warnings); `qa:onboarding` **PASS** (39 checks); `qa:responsive` **PASS** (45 checks, 0 failures); `packaging:smoke --platform=win` and `artifacts:validate --platform=win` **PASS**; working tree unchanged by the runs.


### Cycle-26d queue deltas (signed RC produced and real-host acceptance passed)

- **RC produced:** tag `v2.0-build204-rc4` (lightweight) at commit `1327672b07acad3eace597a883a589872ca2272c`; CI run 36115035070 success (all five jobs); Linux `rc:validate` 292/292; Authenticode verification passed for 89 executables; all 10 release assets uploaded as a prerelease (`isPrerelease:true`, `isDraft:false`); `SHA256SUMS` 9/9 match; `update-manifest.json` 2.0/204 consistent; tag↔commit exact. Setup installer SHA-256 `d5beb6ce4851542950efb24b268a5f32639b72aff4d54790d076c6ca6e5fb81e`; portable `38e8f83fe5960fbee4bb8eb5a0ee00fc93ccb62d37a9b7bb63830feaa7be92e2`; deb `2b3addbf2d9d6eafbb14d3e2b0ebcdac94e2693a8951feadb10071edc90884f4`; AppImage `09d5ea5fed37285e6f30a39374438da1047b169530348524f965391eb8c8f641`.
- **The real-host gate caught a release blocker and it was fixed:** rc3 install aborted with Agent code 24 — the packaged `local-agent-runtime` shipped only `dotenv` while the runtime graph had grown to require `js-yaml` (docker policy) and `ssh2` (backup destinations); build 203 predated those modules. Fix `0d4634b` adds a dependency-closure audit + staging step, general bundling (8 packages), manifest/`agentRuntimePayload` sync, a defensive diagnostics require, and two regression gates with teeth. rc4's shipped payload was proven by extracting the actual installer: closure zero missing and the packaged Agent boots + serves `GET /api/v1/health` 200.
- **CI-retry fixes:** `instances:delete-race:smoke` (Windows-only) and the instance smokes using `process.execPath` now skip/fix cleanly on Linux CI (allowlisted-executable helper); a 293-alias Linux-compatibility audit and a WSL reproduction confirmed only environment-only failures remained.
- **REAL-HOST ACCEPTANCE (rc4, no OS reboot per owner):** Phase 0 pre-capture PASS; Phase 1 in-place upgrade 1.9/203 → 2.0/204 silent exit 0 (68 s), ARP/exe 2.0.0, runtime deps present; Phase 2 installed-binary `qa:acceptance` PASS (0 renderer errors) and real-profile check PASS (onboarding correctly absent; shell renders; Dashboard nav works; paired node identity preserved — the first attempt's "Dashboard must be the landing page" assertion was over-strict: last-page restore is by design); Phase 3 persistence PASS (task enabled/Ready/logon trigger; stop→Ready 1.7 s; start→health 200 2.7 s; relaunch usable); Phase 4 repair/reinstall silent exit 0 (59.7 s), version unchanged; Phase 5 uninstall silent exit 0, install dir + ARP + Agent task removed, **user data intact** (721/721 and 124/124 files; 6/7 durable files hash-identical; `security.json` changed only by legitimate session timestamps). Repo untouched.
- **Deferred:** OS-reboot persistence leg (owner prohibited reboot during work); second-machine replication; Debian/clean-host acceptance; rc3 broken prerelease cleanup (its installers must not be installed).
- **Verdict:** `🟢 READY FOR PUBLICATION` pending the owner's `🚀 publish`; the app is currently uninstalled on this host (data intact) and rc4 can be reinstalled on request.
- Note the accumulated retry tags: rc1 (Windows-only smoke on Linux CI), rc2 (`process.execPath` allowlist), rc3 (agent runtime closure), rc4 (passed) — each prior tag left unmoved.


### Cycle-25 queue deltas (acceptance sweep, frozen-SHA audit, and the runtime proof)

- **ACCEPTANCE queue — three items MOVED, two EXECUTED WITH PASSING EVIDENCE, and one of them was a false assumption worth recording.**
  - **Docker engine gate — EXECUTED, PASSED.** The prerequisite is genuinely present (Docker Desktop 4.84.0, engine 29.6.2, daemon reachable), and `docker:smoke` drives the real socket: `Docker API OK: 0 containers, 6 images`, exit 0. **PARTIAL**, though: the wider live list (bad image, occupied port, missing volume, failed update) has no single harness and would mutate container state, so it was not run.
  - **Two-node drill — EXECUTED, PASSED.** It does **not** need a second host: `scripts/multi-node-fleet-smoke.js:23-29` spawns two real Agent child processes on two free ports with distinct runtime identities. All seven legs pass (distinct deviceIds, credential isolation both directions, kill agent B while A keeps polling, offline node → honest transport error, no cross-node leakage, offline-delete revocation honestly failed, restart with in-flight job reconciled to `FAILED/JOB_INTERRUPTED`, rejoin with a rotated credential). Genuinely-not-live legs stay recorded as such: the transfer leg (waits on I3/I4) and the per-OS legs.
  - **Browser DNS-rebinding probe — a real browser WAS driven, and the claim still stays UNPROVEN.** Real Chromium 149 was driven against a spawned loopback Agent: concrete bind → `421 HOST_NOT_ALLOWED` with the cross-origin read blocked by CORS; wildcard bind → `200` (reproducing §6.1's accepted gap) but the response still unreadable (no `Access-Control-Allow-Origin`); a no-preflight `POST` reached the Agent and was refused `CROSS_ORIGIN_DENIED / origin-host-mismatch`. **The limit is decisive: the attacker page's origin was cross-origin to the rebinding-shaped Host, whereas a true rebinding attack collapses page and target into the SAME origin — which is exactly what would make `origin-host-mismatch` pass.** Reproducing that needs DNS control, which is not available without changing machine state. So the standing rule holds: browser exploitation remains UNPROVEN, and code reading plus cross-origin probes do not upgrade it.
  - **Operator walkthrough — PARTIALLY EXECUTED.** `qa:acceptance` ran clean (`"pass": true`, 11 checks, 0 failures): launch, QA mode, owner gate, and navigation to all eight major pages. That proves the app opens and every page navigates. It does **not** cover config-write or recovery, so the item is not closed.
  - **Onboarding on a fresh install — NOT VERIFIED, and an inference was corrected.** `qa:acceptance` handles onboarding **defensively**: `if (await welcomeSkip.count())` dismisses it (`scripts/qa-acceptance.js:176-191`) and asserts **nothing** about it, recording no result. A harness that *tolerates* a flow is not a harness that *verifies* it, so this item remains open. The earlier inference that `qa:acceptance` covered it was wrong and is withdrawn.
  - **Clean-host Debian 12 install and the live Linux self-update swap — EXTERNALLY BLOCKED.** No Debian host or distro is present (WSL has only `Ubuntu-24.04`, which is neither Debian nor a *clean host*), and the swap needs a real Linux host with systemd plus a version-delta artifact pair. The hermetic half of the swap is covered and passing (`agent:self-update:smoke`). **Unblock requires hardware the owner must provide.**
- **SECURITY queue — the frozen-SHA audit at `ff9d10a` is COMPLETE, and its verdict is NOT VERIFIED.** The deep scan **completed and sealed** this time (`sha256:2ff51196…`, 1012 findings, 255 packages) — but every finding is **unvalidated (0 investigated)**, `verdictEffect: none`, evidence boundary `static_only_no_runtime_execution`. Under the standing rule that a scanner result may not be promoted to a clean verdict, the posture for `ff9d10a` **cannot be called verified**. Three limitations were re-confirmed unchanged and must not be softened: the wildcard bind is still the standalone Agent default; loopback trust is still absolute and an on-host reverse proxy still defeats both gates; and **the remote-administration policy is still not wired, so it protects nothing at runtime** (now recorded in the threat model as §6.7, because it was not mentioned there at all and its header reads like a live control). Verified as clean: secrets (no tracked `.env`, no credential literals — the one real-looking value is a Supabase *publishable* key, which is intended public material), renderer isolation, the host-trust ordering, and the new wave's modules — the payload-byte instrument records **only a length, never content**.
- **Documentation-accuracy queue — two defects fixed.** The threat model cited renderer-flag lines that were ~11 lines stale after the startup refactor (claims re-verified, numbers re-anchored), and its "external-URL allowlist" wording **overstated a control**: it is a *protocol* allowlist (`http:`/`https:`/`mailto:`), not host-restricted, and `allowedHosts` is absent at every call site. Wording, not a vulnerability — but the wording was wrong.
- **WIRING queue — the disclosed gap is now CLOSED with runtime evidence.** The previous wave's honest limit was that the residual `main.js` pins prove only that the call text exists. A release-tier suite now boots the real Electron app with an isolated profile and proves three of four links **at runtime**: the instrumentation wrapper is really installed (a channel registered *after* it emits `IPC request completed {durationMs}` with a shared correlationId), the `instances:list` reply really publishes a snapshot (read back as non-null from the live module instance), and the scheduler really starts with the extracted collector — including the reuse path, with a falsification control so the assertion is not vacuous (an absent node **does** call its fetcher). **The fourth link stays UNPROVEN and is reported as such:** `payloadBytes` appeared on 0 of 23 real completed records, because every reachable shipped channel returns an object and the instrument deliberately skips objects rather than re-serializing them. Closing that would need a string-returning shipped channel or a testability hook — neither is worth adding for the diagnostic.
- **RENDERER queue — four surfaces given UI, three complete and one honestly partial.** Catalog transfer and audit export already had IPC *and* preload; only the UI was missing. Provenance needed a new channel (the existing `dependencies:getCatalog` returns the *node host's* catalog and would have misrepresented it). **Workload trust tier cannot persist, and the UI says so instead of faking a save:** the trust declaration is validated at write time and never stored on the instance record, so the panel renders the live tier ladder and evaluates a described declaration against the real policy in main, with the persistence limit stated on the panel. `permission-matrix:smoke` now reports 59 families / 303 channels and `renderer-free-identifier-smoke` stays at 0. **Status PARTIALLY VERIFIED: no live-window acceptance of the four panels has been performed.**

### Cycle-24 queue deltas (wave 4: the delete race reproduced, and the wiring gap closed)

- **RELIABILITY queue — the Windows delete race is now REPRODUCED, not inferred.** Cycle 23 opened this because retries helping in `deleteInstance` was an inference. It is now a measurement: a real child process holding a handle inside the target tree, then a recursive async `fs.rm` — **without retries 10/10 attempts fail with `EBUSY` and leave the directory behind; with the retry options 10/10 succeed** (30/30 pairs across three runs, consistent). `maxRetries: 1` was added to both recursive calls, matching the existing project model at `maintenanceService.js:259`.
- **Honest correction to the audit's premise.** The mechanism that reproduces is a **live handle holder**, not the post-exit handle-release lag the audit named — that lag was **not** provokable on this machine (0/10, 0/10). The live-holder case is still genuinely reachable in production: the server child's `cwd` is inside the tree being deleted, the guard is `isProcessAlive(config.pid)` on the **wrapper** PID, a detached runtime can outlive that wrapper (which is why `discoverDetachedRuntime`/`adoptDiscoveredRuntime` exist), and `deleteInstance` does not await the `entry.exitFinalization` that `stopInstanceImpl` performs. `ENOTEMPTY` was never observed in any run — only `EBUSY`.
- **Deliberately bounded, so a real failure still surfaces.** `maxRetries: 1` absorbs a brief release window (measured ~200–350 ms against a 60/150 ms lock). A holder that persists longer still fails loudly as `INSTANCE_DELETE_FAILED` rather than being silently masked — the retry is not allowed to convert a genuine lock into a success.
- **AGENT-DISCIPLINE queue — the wiring gap is CLOSED by extraction, and the old assertion is proven to have been blind.** `main.js` no longer contains the implementation: the IPC instrumentation and the alert state collector are require-able modules driven by injection (`src/ipc/ipcHandlerInstrumentation.js`, `src/services/alertStateCollector.js`), covered behaviourally by `scripts/instance-snapshot-wiring-smoke.js` with **8 mutation cycles**, each failing-and-restored with hash proofs. The headline is `M8`, and the orchestrator reproduced it independently rather than accepting it: replace the collector with an inline stub and **the old `source.includes("startAlertScheduler")` check stays GREEN** — the identifier is still present — while both new assertions fail naming the invariant. That is the exact false negative the `includes()` form was hiding, and `scripts/alert-engine-smoke.js` now uses the comment-stripped exact-expression form instead. **The move was verified to be a move, not a rewrite:** 17 of the 18 non-trivial lines removed from `main.js` reappear verbatim in the new modules, and the 18th is the old call site `instrumentIpcHandlers();`, deliberately replaced by the injected-dependency form.
- **HARNESS queue — the repo's own coverage guard caught our integration, which is the outcome we want.** `permission-matrix:smoke` FAILED the gate on the new `src/ipc/ipcHandlerInstrumentation.js`, because its Phase-4 coverage enforcement requires every `.js` in `src/ipc` to be either loaded by the coverage exercise or declared a known non-registrar. It was classified as a **non-registrar with a recorded justification** — not silenced: the guard's purpose is that no module may bypass matrix enforcement, and this module *declares zero channel literals* (verified), so it has nothing to cover; it instruments whatever gets registered rather than registering anything. The entry carries a comment that it must be removed if a channel literal ever appears there. **This is the second time this campaign's own guard has caught a real integration gap** — the first being an orphaned smoke with no npm alias — which is the strongest argument for keeping these guards strict.
- **OPENED — same shape, not process-adjacent, left unchanged deliberately:** `instanceServiceCore.js:6175` (`deleteInstanceFile`) and `src/services/fileService.js:1238` (async `rm`, `force: false`). Retries would be *honoured* at both (they are recursive) and would not mask the `force: false` contract, since `ENOENT` is not in Node's retry set. They are operator-initiated actions whose failure is already surfaced and retriable by the user, so no change was made.
- **OPENED — the guard's own blind spot:** `scripts/rm-sync-retry-guard-smoke.js` scans **literal** options objects. A delete whose options are built elsewhere (`const opts = {…}; fs.rm(opts)`, a spread, or a computed value) is counted as "options not a literal" (currently 0 sites) and is neither confirmed nor denied. Recorded rather than hidden.

### Cycle-23 queue deltas (smoothness wave 3: an audit that refuted its own team, and two corrections)

- **HARNESS queue — FIXED: the wall-clock retry sweep, corrected twice.** The sweep hardened `rmSync` teardown against Windows handle races. An independent adversarial review then found two defects in it, and both are fixed: (1) **`maxRetries` is ignored by Node unless `recursive: true`**, so **26 of 138 sites carried inert options that advertised a durability the code did not have** — those options were removed, not kept; (2) two sites had retries layered *inside* a hand-rolled retry loop whose author had documented why it existed, silently multiplying a deliberately bounded policy (~1 s → ~8.5 s worst case) — the inner options were removed. **Honest headline: 112 of 138 sites are genuinely hardened, not 138.** A one-site exclusion in `stabilization-ui-qa.js` was arbitrary (that site *is* recursive, so retries are honoured there) and is now included.
- **REGRESSION queue — OPENED then CLOSED: a shipped optimization with no test.** An independent audit mutation-tested the snapshot-reuse guard (`SMOOTH-3010`) by setting `INSTANCE_SNAPSHOT_MAX_AGE_MS` to `-1`; **all four candidate suites stayed green and `grep` found zero suites referencing the guard.** It was load-bearing, in production, and uncovered. `scripts/instance-snapshot-reuse-smoke.js` now fails naming the reuse invariant (10 checks, mutation-proven — the orchestrator reproduced the mutation independently and confirmed the two old suites still pass under it, which is what proved the hole was real). **Residual gap: the suite covers the module, NOT the `main.js` call-site wiring** — see the Item-2 plan below.
- **Scope correction — the `src/**` "31 sites need hardening" claim was WRONG, and scoping it first prevented 30 wrong edits.** Classification before implementation: **category (a) genuinely in the race window = 0. (b) recursive but never process-adjacent = 1. (c) non-recursive, where retries are inert and must NOT be added = 30.** Every one of the 30 is a single-file delete (backup / temp / log / session / probe / `.part`). `agent/**` is genuinely 0. **The audit's actual failure mode (a recursive directory delete right after a child exits) lives in async `fs.rm`, not `rmSync`:** `src/shared/instances/instanceServiceCore.js:4581` and `:4611` in `deleteInstance` — guarded by `isProcessAlive` but not by awaiting the exit finalization that `stopInstanceImpl` performs. That is the real ticket; it is opened, not fixed.
- **Do-not-touch list recorded so a future sweep does not "harden" them:** `src/services/electronPaths.js:38` is the only `rmSync` in `src/**` deliberately **not** wrapped in try/catch — its failure is the proof that the directory is deletable, so retries there would delay and could mask a genuinely locked directory. The fail-closed migration sites delete only a backup the operation itself just created, immediately before throwing `*_MIGRATION_RECOVERY_UNVERIFIED`; retries are inert (non-recursive) and would only add latency before the error surfaces.
- **HARNESS queue — FIXED: the acceptance harness could not report its own failure.** Converting 18 fixed waits to condition-based ones (cycle 22) raised the per-condition budgets to 10 s and the bootstrap to 30 s, but left the **whole-run watchdog at a hardcoded 60 s** — so the watchdog pre-empted diagnosable wait failures with an opaque `"timed out"`, and its force-close then left the profile directory locked, producing an `EPERM` **in the `finally` that replaced the real error entirely** (the run's only output was an EPERM; the verdict was unrecoverable). Both halves are fixed: the watchdog is now **derived** from the budgets it must not pre-empt (so the two cannot drift apart again), and a cleanup failure is reported as a warning instead of replacing the verdict. Verified by a passing run: `"pass": true`, `gridHeight 521`, no cleanup error.
- **EVIDENCE-INTEGRITY queue — our own register carried three false or stale statements, all now corrected.** (1) The `SMOOTH-3003` row and the `SMOOTH-3010` "most-called channel" fact quoted a **whole-file aggregate** (831 calls, 63 ms avg, 229 ms max, 2026-09-05→19) as though it were the 82-minute session — the session had **8 calls, 65.1 ms avg, 117 ms max**. (2) The measurement-gap section still described the payload-byte instrument as a *proposal* after it had landed. (3) **The D-9 dedup's claimed harm was withdrawn:** the sole production consumer (`agent/src/services/backupService.js:543`) already de-duplicates at line 553, and the divergence table itself records the collision as *"Harmless for a backup that de-duplicates paths."* The dedup stays (correct and cheap); the "same tree counted twice" impact was overstated. Recorded rather than quietly edited because a register is only worth anything if its corrections are visible.
- **OPENED — a source assertion that a comment satisfies: `scripts/alert-engine-smoke.js:282-283`** asserts the scheduler wiring with `main.jsSource.includes("startAlertScheduler")`, so removing the real call and leaving the string anywhere (a comment, an import) keeps it green. Same weakness the payload-byte suite had until its blind spot was caught. Fold into the extraction below.
- **OPENED — Item 2 plan, scoped and ready: close the call-site wiring gap by extraction, not by more `includes`.** Move the IPC instrumentation (`main.js:136-166`) and the alert state collector (`main.js:1097-1112`) into require-able modules with dependency injection, cover both behaviourally with mutation teeth (does the wrapped `instances:list` reply publish exactly once? does a throwing publish leave the reply intact? is the resolve call made with the selected node id and the host fallback?), then pin the two residual `main.js` one-liners with the **exact-expression, comment-stripped** assertion technique from `scripts/ipc-payload-instrument-smoke.js`, not `includes`. Rejected: a testability hook in production code (a hook only a source assertion can see closes nothing and adds risk); an Electron-booting integration test is a release-tier backstop, not the guard. This touches the startup path, so it needs a full gate run.

### Cycle-22 queue deltas (smoothness campaign: 37-entry register, waves 1-2)

- **HARNESS queue — a pre-existing FIXED-WAIT in an acceptance script is now an implicit performance requirement on the renderer.** The wave-1 smoothness lane found that `scripts/stabilization-ui-qa.js:209-211` fills `[data-game-config-search]` with "players", waits a **hardcoded 100 ms**, then asserts `max-players` is visible. Any debounce on that input must therefore be **under 100 ms** or the acceptance run becomes timing-dependent — so the lane chose 80 ms instead of the file's usual 120 ms and verified the constraint by reading the script (it did not run it; no Electron). **This is the same class the campaign has already fixed three times in test fixtures** (a 1000 ms health budget, an 80 ms job-expiry deadline, a 6–12 s agent-readiness loop) — except this one constrains production code rather than the test. **Follow-up: convert those fixed `waitForTimeout(100)` waits to condition-based waits** (assert on the field becoming visible, with a bounded poll), so a future debounce is not silently bounded by a test literal.
- **AGENT-DISCIPLINE queue — an ownership breach worth recording because the rule exists for a reason.** Wave 1 edited `package.json` to register its own smoke, against an explicit instruction to *report* the alias so the orchestrator could register it (the rule exists because concurrent lanes editing one manifest is the collision that has bitten this campaign). The change itself is correct and is kept — one alias line — but the breach is recorded rather than glossed, because the discipline only works if violations are visible.
- **Process note — the right way to fix a failing assertion that belongs to someone else.** Wave 1's change broke `node:stale-response:smoke`, which asserts a literal `if (!shouldSkipNodeScopedPolling() && getActivePageName() === "marketplace")`. Instead of editing the other lane's test, it **rephrased its own gate as an early return above that line**, so the asserted literal survives verbatim and the node-scoping guard is untouched. That is the standard: change your own code to satisfy another lane's contract, never the other way round.

### Cycle-19 queue deltas (first runtime acceptance pass)

- **OWNER DECISION OPENED — remote administration is unauthenticated-by-origin today, and the tightening is NOT mine to make.** A lane established, with `file:line`, that **origin is never consulted for authorization anywhere except the pairing gate**: `agent/src/auth.js:54-56` compares one shared bearer token, `agent/src/server.js:537-540` enforces scopes, and `request.socket.remoteAddress` is used only as a rate-limit key (`server.js:437`). The Agent defaults to a **wildcard bind** (`agent/src/config.js:10`), so "reachable on the LAN" and "remote administration" were the same thing: **any holder of the token, or any enrolled credential whose record carries no `scopes`, could reach every authenticated route — including `owner` (enroll/revoke) and `agent:manage` (credential rotation) — from any address the Agent was reachable on.**
  - The lane built the rule and proved it: `src/shared/remoteAdminPolicy.js` fails closed (an undeterminable origin is refused, never assumed local), refuses a remote caller without `AGENT_REMOTE_ADMIN`, refuses a remote **unscoped** credential, keeps the browser-UI family loopback-only, and preserves on-host recovery (`npm run agent:pair` still works, and `/enroll/*` and `/pairing/*` are untouched so re-enrollment is never gated). Two mutations proved the guards.
  - **The orchestrator REVERTED the enforcement wiring (`agent/src/server.js`) rather than shipping it.** Reason: the project's supported topology is a desktop administering remote nodes over LAN with a shared token — documented, smoke-covered, and present in the operator's own environment (a node at `192.168.1.134`). A fail-closed default would require setting `AGENT_REMOTE_ADMIN` on that Agent **and** re-enrolling it with family scopes before the desktop could manage it again. That is a behaviour change to a supported deployment, so it is the owner's call, not an engineering default. **The decision needed: keep the current permissive-by-origin behaviour, require opt-in (the lane's rule), or a narrower version that protects only the credential-operation routes.**
  - **What is kept, therefore:** the policy module and its tested rule, with a registered smoke that runs the **policy table** and states plainly that **request-layer enforcement is not wired** (`REMOTE_ADMIN_ENFORCEMENT_WIRED = false` in the smoke, with the reason in a comment). Wiring it is a small change in one file plus flipping that constant, and the smoke's request-layer assertions are retained so they become the proof when it is wired — skipped and *said to be skipped*, never deleted and never left failing.
  - **Unproven either way:** no remote caller was exercised across a real machine boundary, and the desktop's LAN remote-node administration and remote revoke/rotate paths are covered by no smoke that leaves loopback. Their post-change behaviour was reasoned, not tested — which is a second reason not to have shipped it unilaterally.

- **REGRESSION QUEUE — SYSTEMIC FINDING, ~20 MIGRATION SITES TAKE AN UNVERIFIED RECOVERY POINT.** While closing the V2-J bullet, the lane inventoried **29 schema-migration sites with `file:line`** and established that **every one writes a copy of the pre-migration file and NONE verified it** — the copy was written and immediately trusted, so a truncated, corrupt or mismatched copy would still let the migration overwrite the only good source. Verification (`size` + SHA-256 for byte copies; parse plus decrypt-and-compare for the encrypted re-encode stores) is now wired into **three** stores: instance configuration, node credentials and marketplace/provider config. **The remaining ~20 sites are a known, enumerated gap** — the reusable policy exists, and each site is listed in the lane's inventory with its `file:line`. This is the fourth time this campaign has found a defect class present at more than one site, and the follow-up is a bounded sweep rather than a redesign.
- **The central caveat, stated by the lane itself:** a *verified* recovery point proves the bytes (or logical payload) match the pre-migration source. It does **not** prove a restore would succeed, that the restore path is correct, or that a restore reproduces a working installation. **No restore drill has ever run in this repository** — the smoke's own output carries `"restoreDrillRun": false`. That remains the honest gap in the V2-J bullet, which stays **partially** closed rather than checked.
- **Fail-closed choices recorded:** a pre-existing recovery point that fails verification causes a refusal that repeats on every read rather than being re-taken, because re-taking could overwrite the only copy; and a copy this operation created but could not verify is deleted so an operator never trusts it, while a pre-existing one is never deleted.
- **Mutation evidence:** breaking the "taken but unverified ⇒ refuse" branch fails the smoke naming the invariant; mutating the refusal path to write before throwing fails the `store-unchanged-on-refusal` assertion (the store must stay byte-identical on a refusal). Both restored.

- **ACCEPTANCE QUEUE: no longer 0%.** The app was launched from `3cb829b` and inspected through the real renderer, read-only (no install/start/stop/transfer/apply clicked), then closed cleanly with all five Electron processes terminated and the tree left clean. Five previously code-only surfaces are now **CONFIRMED WORKING at runtime**: the alerts panel and the durable-jobs panel (both cycle 14, with honest empty states and live "0 active"/"0 tracked" pills), the Docker surface (honest `—` placeholders rather than fabricated zeros, plus the volume-deletion warning), the Public Access page (a live Playit tunnel and provider cards including "AnxOS Relay — Disabled" stated honestly), and owner authentication plus diagnostics writing.
- **REGRESSION QUEUE — F1 OPENED (real defect, P1-class): a transient status string is rendered as a metric value.** The Dashboard's Network card (DOWNLOAD / UPLOAD / TOTAL DOWNLOADED / TOTAL UPLOADED) and Runtime card (UPTIME) displayed the literal text **"Switching to Windows Desktop…"** in the value position. Expected: an honest placeholder with the status shown as a banner, not as the value. Evidence: the acceptance capture set. A diagnosis lane is dispatched.
- **RELIABILITY QUEUE — F2 OPENED (needs diagnosis): the node switch never completed** across the whole ~10-minute session ("Anxlab / Switching node…" in the sidebar, top pill alternating Connected/Running). Observed consequences: the Docker page stayed on "Checking Docker daemon status…", the dashboard metrics showed the switch text, and a remote node returned 404 for `/api/v1/jobs`. The app's own runtime state reports `localAgentProcessStatus: "Offline"` while `dockerHealth` for Anxlab reports `daemonRunning: true` with **5 containers / 2 running** — **so a docker snapshot exists that the Docker page is not displaying**. Whether "Switching node…" should time out into an explicit failure is a product question; the lane is asked to establish the mechanism before anyone changes behaviour.
- **REGRESSION QUEUE — F5 OPENED (version skew): the durable-jobs surface assumes an Agent capability older Agents lack.** The remote node returned HTTP 404 for `/api/v1/jobs?limit=100`. The UI degraded to an empty state without crashing, which is correct, but an empty list is not an honest answer to "your Agent is too old for this view".
- **ROADMAP GAP queue — two questions that need a decision, not a guess.** **F3:** the alerts panel says "0 active" while the notification centre shows 4 critical issues on the same day — either the alert condition set is narrower than node health (plausible) or it under-fires. **F4:** the nav badge reads 99 while the panel counters read 111 unread / 112 history.
- **HARNESS queue — acceptance method limitation recorded.** `SetForegroundWindow` is refused by Windows' foreground lock and `AppActivate` returns False, so captures use `PrintWindow` with `PW_RENDERFULLCONTENT` (which renders a window regardless of z-order). Clicks work because they are position-based; **the mouse wheel does not**, because wheel events follow keyboard focus. Consequence: pages reachable by navigation were verified, content below the fold was only partly reachable. A future pass should raise the window properly (e.g. by clicking its taskbar entry) or size the target above the fold.

### Cycle-17 queue deltas (Wave B: reachability, the last absent item, rollback honesty)

- **ROADMAP GAP queue — the V2-E contract lane found TEN divergences between the shipped adapters and any sane contract, and recorded them instead of smoothing the prose.** The ones that matter as queue items:
  - **D-4 (product decision): only SteamCMD adapters have an in-place update.** Minecraft, FiveM and Terraria have no update path at all, and the agent exposes only `/steamcmd/update*`. Whether a Minecraft version upgrade is expected outside a reinstall is an **owner decision**, not an engineering gap.
  - **D-5 (documented limitation): four recognised families — terraria, valheim, rust, cs2 — have no config adapter** and return `UNSUPPORTED_GAME_CONFIG`. The contract now asserts this rather than leaving it implied.
  - **D-10 (uneven verification): every game template declares `installer.verifyFiles` except the Minecraft templates.** Routed to the marketplace lane: decide whether that is deliberate.
  - **D-1/D-2 (design): readiness is one shared log regex plus a port probe, not a per-adapter capability**, with two game-specific behaviours implemented as family string branches. They work, but a new adapter cannot declare its own readiness signal.
  - **D-8: no version pinning outside SteamCMD** — Minecraft persists `minecraftVersion` and nothing compares it.
  - **D-3: installation is three unrelated mechanisms** (core installation sessions, the separate SteamCMD session protocol, generated marketplace scripts).
- **REGRESSION queue: D-9 FIXED, impact wording CORRECTED.** The Palworld world-scope candidate list carried a duplicate — with the default install directory the first entry normalised to exactly the second — so the list named the same tree twice. De-duplicated at the point the collision is produced. **An independent audit then refuted the impact originally claimed for this fix:** the only production consumer (`agent/src/services/backupService.js:543`) already de-duplicates at line 553 before doing any per-candidate work, and the divergence table (`docs/v2/V2_GAME_ADAPTER_CONTRACT.md:487`) had itself recorded the collision as *"Harmless for a backup that de-duplicates paths."* No consumer summed sizes per entry. The fix is correct and cheap and stays; the "same tree was counted twice" harm was **overstated and is withdrawn**.
- **HARNESS queue: an ORPHANED SMOKE WAS FOUND AND WIRED.** `scripts/game-server-config-smoke.js` — the only smoke exercising the config-adapter write path directly — had **no npm alias**, so `rc:validate` had never run it. Registered as `game-server-config:smoke`. This is the third time this campaign has found a test that existed but was not in the gate; treat "the suite passes" as a claim about the suites that RAN.
- **Reachability queue: the catalog-transfer and audit-retention services are now reachable from the app** (five channels + preload exposure, with matrix rows), verified by two-layer behavioral smokes that load the real preload and register the real IPC modules. Two honest limits recorded: **catalog import does not persist** (no write channel exists, so a renderer must consume the result through whatever path the app defines), and the **tier choice is a judgement** — the catalog service enforces no tier, so both channels were placed at read tier by analogy with the read-only install-plan preview. If importing a foreign catalog should be install-tier, that row changes.

### Cycle-16 queue deltas (Wave A: closing the absent V2-H/D/I/J items)

- **AGENT-DISCIPLINE queue — NEW RULE, learned the hard way: dependency state is a shared resource and only the orchestrator may mutate it.** During Wave A a lane ran a targeted `npm install <pkg> --no-save --no-package-lock`, which **pruned `node_modules` to zero** and broke the tree for every other lane for a window (it was already partial at 103 entries before that). The lane self-reported the action unprompted, repaired it with `npm ci`, and verified recovery (230 packages, Electron binary present, manifests untouched) — which is the behaviour that made this diagnosable rather than mysterious. **Rules adopted:** no lane may run `npm install`/`npm ci`/`npm uninstall` or otherwise change dependency state; a lane that hits a genuinely missing module reports it and stops. Only the orchestrator restores the environment, and only after every lane has stopped.
- **HARNESS queue — two full-gate failures in that window are UNATTRIBUTED, not "flaky suites".** `agent-control:smoke` failed at 244/254 with `Cannot find module '@electron/get'` from Electron's postinstall path, and `fleet:aggregation:smoke` failed at 97/254; both pass standalone. The first is explained by the dependency window. The second is **not attributed** — it may be the same window or load-related, and guessing would be worse than saying so. Integration gates are now run only on a quiet tree with all lanes stopped; if either suite fails there, it gets root-caused like the earlier environmental flakes.
- **Process note:** the orchestrator's own first response to the dependency report was wrong — it checked the post-repair state and generalised backwards, telling two lanes their environment observation was a working-directory artifact. **A verification performed after someone else fixed the problem does not retroactively invalidate their observation.** Check the timeline before ruling a peer's finding wrong.

### Cycle-15 queue deltas (re-freeze at 3f7c16e, delta re-audit)

- **Frozen candidate MOVED to `3f7c16e`** (from `4749e56`). Gate at that SHA: **254/254 PASS**. Security delta verdict: **ACCEPTABLE WITH DOCUMENTED GAPS — no new P0/P1/P2**; both hardening fixes PROVEN BLOCKED under adversarial probing, the three accepted gaps confirmed unchanged, and neither fix widened the attack surface.
- **SECURITY queue: the Mimosa scan at `3f7c16e` is INCOMPLETE evidence, not a pass.** completeness `partial`, runStatus `inconclusive`, seal `sha256:90caaead…`, 955 findings, gap = call graph partially incomplete (dynamic reachability may be incomplete; source coverage itself was complete at 495/495). **No security claim attaches to it.** Its 7 delta-relevant findings were triaged: 2 ACCEPTED PATTERN (`vm` on repo-owned source in dev-only smokes) + 5 FALSE POSITIVE (`showToast` textContent taint, a loopback fixture `fetch`), 0 exploitable.
- **DURABLE RULE recorded for `vm.runInContext` in tests:** acceptable only when the executed string is a repo-owned artifact read from a module-load-fixed path, with no byte from argv/env/network/node/user, and the sandbox grants no real capability. It becomes unacceptable as soon as an externally influenced byte reaches the code string, the context gets real capability, or the file ships in the packaged app.
- **TEST queue OPEN (new, from the delta audit's coverage statement):** `scripts/amp-panel-url-gate-smoke.js` does NOT cover tab/newline-encoded schemes, protocol-relative `//host`, `https:`/`https:/` normalisation, control/unicode characters, a host-trust assertion (any http/https host is allowed by design), or the two sibling `href` sinks. The gate itself survived all of those under probing; the smoke should be extended so a regression in any of them fails the build.
- **REGRESSION queue OPEN (routed, pre-existing, UNPROVEN):** `app.js` assigns `href` from `instance.connectionHost`, which is length-capped but not scheme/host-validated (`http://` is pinned, so no script execution; needs `instance:write` plus the same actor clicking). Route to the renderer lane.
- **DOCS/CONTRACT queue OPEN (routed):** `instances:jobs:list` accepts `nodeId` but always queries the local job store — the comment in the preload/renderer says jobs are owned by the selected node. Not an authorization bypass, but the contract and the comment disagree. Route to `api-contract-guardian`.
- **HARNESS queue OPEN:** the scanner's sealed-scan CLI (`payload/dist/cli.js security-scan run --deep`) is the path to use when the MCP tools are unavailable after a restart; record scanId + sealDigest + completeness in the campaign log each time, and never treat `partial` as a pass.

### Cycle-14 queue deltas (frozen-commit audit of 4749e56)

- **HARNESS queue — a TIMING CLASS, closed. Three instances, one root cause: a fixture margin so tight it doubles as an environmental test.** (1) `node:independent-health:smoke` asserted classification with a 1000 ms HTTP budget across seven concurrent fixtures (failed 152/252 under load). (2) Nine Agent-readiness loops used 6–12 s budgets that failed under load and reported "did not become ready" whether the Agent was booting or dead — they now share `scripts/test-helpers/agent-readiness.js` with a generous budget, child-exit fast-fail (exit code/signal + stderr tail) and the last probe error. (3) `instances:job-expiry:smoke` minted an 80 ms deadline that had already passed by validation time (failed 74/254 with `INVALID_JOB_EXPIRES_AT`); its assertions are about ordering, so margins became 1500 ms and the already-expired fixture states a past timestamp directly. Proven under artificial load on two suites; the rest inherit the helper and were verified to pass normally — stated as inherited, not claimed as individually load-tested. Gate after: 254/254.
- **LESSON ADOPTED (extends the cycle-12c rule):** a fixture's timing margin is part of its contract. If a budget can fail because the machine is busy, the gate is testing the machine. Tight budgets are legitimate ONLY where the tightness is the assertion — and those must be commented as such.

- **SECURITY queue: Mimosa COMPLETE against the frozen SHA** (scanId `scan-2026-09-18T18-30-03.134Z-0db48aa4ad76`, seal `sha256:a546e102…`), 948 findings triaged onto campaign lines: **40 on added lines → 39 false positive, 1 accepted pattern, 0 exploitable.** Static-only evidence boundary; no runtime execution claimed. **CLOSED for this SHA.** Open sub-item: the 4 offline dependency advisories are **UNPROVEN** — the scanner persisted counts but no identifiers, so nothing can be assessed until a re-run with identifiers enabled.
- **SECURITY queue: three P3 hardening items from the adversarial lane.** N1 (`%`-truncation validation/use mismatch) and N2 (duplicate `Host` refusal was a dead branch) **CLOSED** with mutation-proven pins in `agent:host-trust:smoke`. N3 (AMP panel `href` accepted an unvalidated agent-supplied URL, so a malicious node could return `javascript:`) **delegated to the renderer lane** — UNPROVEN exploitability, real risk, hardening fix.
- **REGRESSION queue: the gate itself was the first defect found.** `node:independent-health:smoke` was flaky — a 1000 ms budget for real HTTP round trips across seven concurrent fixtures, so machine load produced false failures (failed at 152/252 in the audit run, passed in isolation). **CLOSED:** success-expecting budgets widened with a comment explaining that the assertions are about classification, not latency; the deliberately-tight closed-port leg is kept and commented. Fail-without proven under artificial CPU load (exit 1, multiple `AGENT_TIMEOUT`), pass-with under the same load (exit 0).
- **RELEASE queue: the website advertised a release that does not exist.** `website/config.js` named tag `v2.0-build203` and `2.0-build203` asset names — six 404 URLs — because the sync derives URLs from `release.json` while build 203's artifacts were published as `1.9-build203` under `v1.9-build203-rc2`. **CLOSED (data + guard):** config corrected, and `verify-website-config-vs-stable.js` now compares the tag and every configured asset file name against the actual stable release instead of only the numeric build. Proven both directions against the live release.
- **RELEASE queue OPEN, OWNER-GATED (needs explicit approval — a workflow change):** the publish job uploads the raw `rc-validate.log` as a permanent public release asset (it contains CI runner paths and a fixture IP; no credentials), and `validate-release-artifacts.js` does not scan `.log`. Also owner-gated: no RC exists for this commit, signing-secret provisioning, and the ADP install/acceptance gates. Recorded, not touched.
- **ROADMAP queue: 10 registered channels unreachable (HIGH).** `alerts:list|acknowledge` (the operator cannot see or acknowledge an alert, so V2-J bullet 3 was hollow), `instances:jobs:list|get|cancel`, and five `backups:*Destination` channels. **Alerts + jobs lane DISPATCHED**; the destination channels are recorded as a documented deferral (API_SURFACE_V2.md already said "no renderer control yet").
- **DOCS queue: four contradictions corrected** (operator notes §5/§7/§9, support matrix, API surface omissions) plus a new operator section on the downgrade limitation (schema-2 instances are unreadable in a pre-Build-200 build — data preserved, functionality degraded), and the `MASTER_ROADMAP.md` §6 status pass: 47 bullets flipped to `[x]` **only** where code plus a registered hermetic smoke exist, with every acceptance gate left NOT VERIFIED and the genuinely absent items (publisher-trust warning, catalog export/import, adapter contract, reverse-proxy/certificate lifecycle, alert surfacing) left open. Also corrected: `docs/KNOWN_LIMITATIONS.md` still claimed the install-plan preview was API-only.
- **Standing lesson reinforced:** the frozen audit's most valuable outputs came from lanes attacking the *evidence* rather than the code — a flaky gate, a guard that compared the wrong field, a "missing feature" that was a coverage gap, and a roadmap that understated rather than overstated. Keep auditing the auditors.


### Cycle-13b queue deltas

- **SECURITY queue: F4 CLOSED (Host trust + cross-origin), with one accepted gap.** The Agent now validates `Host` on every route before authentication (`HOST_NOT_ALLOWED`, 421, fail-closed, hostile host never echoed or logged) and refuses state-changing cross-origin requests (`CROSS_ORIGIN_DENIED`, 403). The pairing `agentUrl` no longer echoes a caller-controlled host, and the request URL is parsed against a fixed internal base. Covered by `agent:host-trust:smoke` with fail-without evidence, and the browser UI's own bootstrap → session → page flow was verified end to end against a real spawned Agent rather than by asserting a header's absence.
- **ACCEPTED GAP (must not be forgotten): a wildcard bind (`AGENT_HOST=0.0.0.0`/`::`) is not strict** — any syntactically valid `Host` is accepted there, because deciding whether a name resolves to a local address needs a request-path DNS lookup. On a wildcard bind a rebinding page's `pairing/start` would still succeed; its `agentUrl` no longer reflects the hostile host, and the cycle-12 pairing gate still applies to an enrolled node. The strict allowlist requires a concrete bind. **Follow-up option recorded, NOT implemented: an operator-configured allowed-host list** (the desktop already stores an unused `allowedOrigins` setting) — this is the only remaining way to make a wildcard bind strict without a DNS lookup, and it needs an owner decision.
- **SECURITY queue OPEN (low, recorded):** loopback trust remains absolute, so an on-host reverse proxy in front of the agent port defeats both the pairing gate and the Host allowlist; same-origin is compared host+port only, so a scheme mismatch behind a TLS-terminating proxy is not detected (a mismatched-origin page still cannot read responses or change state).
- **SECURITY queue OPEN (recorded limitation):** a remote Agent reachable only through a DNS name must have that name in its `agent.json` `agentUrl` or requests to it are refused. A remote Agent reached by IP needs no configuration. Documented for operators.
- **UNPROVEN and staying that way for now:** whether a real browser could ever complete the DNS-rebinding read, and whether this change defeats it in-browser rather than at the socket. No browser was driven; a browser-based probe would need a lane with a real Chrome and a rebinding harness.

### Cycle-13 queue deltas

- **HARNESS queue: H7b CLOSED (correctly scoped this time).** It was recorded as "listBackups read-path legacy-schema tolerance" — a suspected missing feature. The tolerance already existed, so the real gap was coverage; writing that coverage found a **second site of the same race** in the schedule-store migration (`readSchedules`), which had the identical stat-then-copy pair and could take the schedule store down when two reads raced. Both sites now use the atomic `COPYFILE_EXCL` form with EEXIST tolerated, and `backup-metadata-migration-smoke` covers both with deterministic lost-race legs and PROVEN teeth (reverting either site fails the smoke — the metadata leg with its own invariant message, the schedule leg with the raw EEXIST).
- **Lesson recorded for the queue:** a "missing feature" queue item can be a coverage gap, and verifying it can surface a real defect the item never mentioned. Verify the premise of a queued item before implementing it — and if the premise is wrong, say so and re-scope rather than building the thing that was asked for.
- **Tooling constraint recorded:** Mimosa's path-traversal rule blocks edits to files that already contain an unvalidated `path.join(root, variable)` pattern (this includes the existing backup smokes, so they can no longer be extended). The workaround used is a containment-checked path helper in the new file — never a scanner change.

### Cycle-12c queue deltas

- **TEST queue — LESSON ADOPTED AS STANDARD: static string pins are decoration, not coverage.** The UI reviewer independently verified the six fixes made in response to its findings (6 of 7 PROVEN-FIXED) and then MUTATION-TESTED the new pins in `ui-polish-smoke.js`. Result: **a future edit that stops honouring the typed confirmation (`if (!confirmed)` → `if (false)`) passes all seven pins**, as do a second destructive path written as `api.workload.transfer(`, a phrase derived from raw input instead of the preview-resolved id, and a neutered re-check with its toast string kept. Only the consistency-disclosure and anchor pins have real teeth. **Rule for the rest of the campaign: a guard invariant is only covered by executing the flow and asserting on observed calls — never by asserting that a string appears in a file.** A `transfer-gate-behavior-smoke` lane is dispatched to build exactly that (declined confirmation ⇒ transfer never called; accepted ⇒ transfer called with the RESOLVED id; preview failure ⇒ no confirmation; node switch ⇒ result not swallowed; a second call site ⇒ smoke fails), with the three evasions as its acceptance criteria.
- **Closed this cycle:** the transfer button now mirrors its disabled reason into `aria-description` like the install-plan button (the reviewer's one PARTIALLY-FIXED item: the button is disabled in most states and Chromium does not show a `title` tooltip on a disabled control). Two nits left as-is and recorded rather than churned: the confirmation message has no separator between the last step and the closing sentence, and an unprobed node reads "— Connecting" rather than "— Offline" (pre-existing fallback, errs toward annotating).
- **Re-verified, no new defects:** the reviewer found no regression from the six fixes (no second destructive path, guard ordering intact, single call site still 1, no new DOM sink — `renderer-safety:smoke` and the injection harness re-run clean over the current tree, network inventory untouched).
- **Process note for future requests:** `npm run node-switch:smoke` does not exist — the script is `node:switch:smoke`. A missing-script invocation exits 1 with no output, which reads like a regression; check the name with `npm run` before treating a silent failure as a finding.

### Cycle-12 queue deltas

- **SECURITY queue: F1 / F1b / F2 / F3 CLOSED (43aeef7).** The pairing surface now authorizes on an enrolled node — loopback origin or a credential the agent trusts, else `PAIRING_REQUIRES_EXISTING_CREDENTIAL` (403) — evaluated before any mutation, with first pairing and post-revocation recovery unchanged. Covered by the new `agent:pairing-credential-gate:smoke` (6 legs, teeth proven by disabling the gate), and the permission-matrix contract row for the pairing status route was updated with added teeth. **Lesson recorded: the enrollment-gate-only fix this campaign was about to make would have closed nothing**, because the record self-heals to the live credential; the authorization rule belongs on the surface that installs the credential.
- **REGRESSION queue: one PRODUCT REGRESSION introduced by the security fix, disclosed and being fixed.** The desktop's own remote repair does not present the credential it holds (`agentControlService.startPairingSession` posts no header; `nodeService.postPairingComplete` posts only code+token), so remote re-pair of an enrolled node fails even when the desktop HAS a valid credential. No smoke catches it (`node:remote-pairing-target:smoke` runs its own mock server; `multi-node:fleet:smoke` re-pairs over loopback only). **Follow-up lane DISPATCHED** to attach the stored node credential to both calls. The unrecoverable case (remote node whose credential is LOST) stays an on-host action by design and must be documented for operators.
- **SECURITY queue OPEN (new, not yet scoped): F4 — Host header trust + no CORS.** The agent builds the returned `agentUrl` from `request.headers.host` and sends no `Access-Control-Allow-*` headers. The DNS-rebinding precondition is proven; whether a real browser can complete the attack is UNPROVEN. Candidate fix: a Host allowlist (loopback + configured agentUrl) and explicit CORS denial. Needs a lane; low urgency until proven browser-reachable.
- **SECURITY queue OPEN (recorded limitation):** loopback trust is absolute, so an on-host reverse proxy in front of the agent port would make remote callers appear loopback. A corrupt `enrollment.json` now fails the pairing routes closed (500) instead of allowing pairing — intended, but untested.

### Cycle-11 queue deltas

- **SECURITY queue: one P1 OPENED, PROVEN, and deliberately NOT closed** — now REFINED by a read-only security lane, which found the exploit is CHEAPER and the fix surface WIDER than first recorded. Severity depends entirely on reachability:
  - **F1** — `pairing/start` is pre-auth and returns the code to the same caller; `pairing/complete` sets the live `config.token` to a caller-chosen value; that token then authenticates. Reproduced: control refused 403; `start` 200 with a code; `complete` 200; `GET /instances` with the installed token 200; enrollment record rebound with caller-chosen scopes.
  - **F1b (NEW, changes the fix surface)** — the enrollment record **self-heals** to the live fingerprint on the next authenticated request (`enrollmentService.js` ~546-563, deliberate rotation tolerance). So `/enroll/complete` is NOT required: the minimum exploit is three requests and it never touches `assertRepairAuthorization`. **Any fix confined to the enrollment gate is ineffective**, and the earlier "pair-then-enroll is required" framing was understated.
  - **F2 (NEW)** — `GET /api/v1/pairing/status` returns the live pairing code pre-auth.
  - **F3 (NEW)** — `POST /api/v1/pairing/cancel` is pre-auth; an unauthenticated caller can destroy the operator's pending session (single module-global slot).
  - **F4 (NEW, precondition proven / browser step unproven)** — the Host header is trusted when building the returned `agentUrl` and the agent sends no CORS headers, so a DNS-rebinding origin could be treated as same-origin. Whether a real browser can complete this is UNPROVEN.
  - **Reachability verdict (decides severity):** a default desktop-spawned agent binds `127.0.0.1` and AnxOS ships NO inbound firewall rule for the agent port, so a default install is **not** remotely reachable → local-process attacker only (P2). A STANDALONE agent defaults to `0.0.0.0` (`agent/src/config.js`), and remote nodes over LAN/Tailscale are a documented, smoke-supported flow → **P0** in that topology.
  - **Fix lane DISPATCHED (Option B):** for an ENROLLED node the pairing routes (`start`, `complete`, `status`, `cancel`) require either a loopback origin or presentation of the credential the agent already trusts, with a new `PAIRING_REQUIRES_EXISTING_CREDENTIAL` (403); first pairing (no record) and post-revocation stay open. Accepted loss, to be documented: remote re-pair with a LOST credential becomes an on-host action (the product already supports this via `npm run agent:pair` / `scripts/agent-pair-export.js`). Options A (loopback only), C (on-host code generation) and D (refuse re-pair of an enrolled node) are recorded in the lane's report with their blast radius; C is the correct long-term shape but requires redesigning the remote "Generate pairing code" action and rewriting `node:remote-pairing-target:smoke`.
  - **NOT to be re-litigated:** the live-credential acceptance in `assertRepairAuthorization` stays — the legitimate repair flow pairs a node (rotating the shared token) and then enrolls with the rotated credential while the record fingerprint is stale, and `multi-node:fleet:smoke` fails 403 where it asserts 200 against a record-bound gate.
- **REGRESSION queue:** the P2 workload-log misattribution is CLOSED (an operation scope leaked onto a run's long-lived pipes; the run now owns its id and both pipes stamp it explicitly, smoke-pinned). Backup metadata migration race CLOSED (atomic `COPYFILE_EXCL`). UI review P1 (source-backup consistency undisclosed at the destructive gate) + P2s (offline target labelling, install-plan node staleness) + P3s (guard re-check after await, unguarded success-path refresh, misleading comment) CLOSED with pins.
- **REVIEW queue: R9 (UI) and R10 (correlation + enrollment) CLOSED.** R10's recommended tightening was implemented and then **DISPROVED BY EVIDENCE** — `multi-node:fleet:smoke` failed 403 where it asserts 200, because the legitimate repair flow pairs a node and then enrolls with the rotated credential while the record fingerprint is still stale. Reverted; the acceptance is now pinned in `agent:enroll:smoke` so a future tightening cannot land silently. Recorded so the next reviewer does not re-litigate it.
- **HARNESS queue:** H7 ("listBackups read-path legacy-schema tolerance") **CLOSED AS STALE** — the tolerance already existed (v0→v1 migration with a byte-stable `.schema-v0.backup`); the real gap found while verifying is *coverage*. Replaced by H7b: pin legacy backup metadata migration (migrated on read, backup byte-stable and written once, idempotent across list+restore, and a future schemaVersion refused with `BACKUP_METADATA_SCHEMA_UNSUPPORTED`).
- **TEST queue (new):** denied Agent actions carry no correlation id — deliberate (adopting a header id pre-auth would let an unauthenticated caller choose a log field), so it needs a documented decision rather than a fix; `normalizeJoinKey` accepts token-shaped values (hardening, no untrusted caller today); the correlation smoke's wiring phase is still partly grep-based; auto-restart / version-refresh timers can still inherit an operation scope.
- **DOCS/STATUS queue:** cycle-11 heartbeat written; the ~950 new renderer lines have no runtime evidence and are flagged as such in the log and the live-acceptance queue.
- **FINAL AUDIT queue:** Mimosa's pre-commit scan returned `scanner_enobufs` (incomplete) twice this cycle — **no security claim is made for cycle 11**, and the full re-audit at a frozen commit remains owed.

### Cycle-10 queue deltas

- **Implementation queue: 4 waves CLOSED** — V2-H wave 2 (firewall lifecycle), V2-I wave 2 (scoped agent tokens enforced), V2-F wave 5 (backup destinations: local + SFTP + AES-256-GCM + restore-from-remote), V2-J wave 1 (alert engine + IPC). Cycle 11 lanes dispatched: **UI reachability** (the 3 unreachable surfaces), **enrollment re-pair hardening** (security), **V2-J correlation IDs**.
- **Regression queue: the P0 is the headline.** A declined transfer preview deleted a pre-existing target instance (adversarial audit reproduced it; the docs curator found it independently while verifying behavior). Fixed + smoke-pinned. Also fixed: remote path traversal (backslash separators), job-store temp collision, restore verify-then-extract TOCTOU, matrix indirect-registration hole, renderer attribute injection, dead-code restart, alert engine dead feature, destination temp collision, firewall rule identity.
- **Review queue: R8 CLOSED** (three P1s addressed; two recorded for owner-scoped contract decisions below).
- **Harness queue: H1/H4/H6 CLOSED** with proven tripwires. Remaining: H2 (source-pin re-tiering), H3 (bootstrap vm-extraction), H5 (website:smoke split), H7 (listBackups read-path legacy-schema tolerance).
- **Security queue: S1 still open** — the sealed triage covers the baseline; the campaign's new surfaces were individually audited by the security lane this cycle (zero P0/P1 there) but a re-scan at a frozen commit is still owed for the final audit. **NEW S6 (recorded, owner-scoped):** the enrollment completion flow accepts a re-pair with only the nonce + a self-chosen token, so scopes can be dropped without the existing credential — pre-existing V2-A design that makes the scoped-token guarantee non-binding against a caller who can reach the agent port. Cycle 11 lane dispatched to harden it. **NEW S7:** the firewall elevated-confirmation/rollback guard is desktop-only; a direct agent caller can create an access-affecting rule with no guard (needs the api-contract lane).
- **Reliability queue: Y1/Y2 CLOSED** (scheduler CRUD merge; busy-job skip with fail-closed query handling). Remaining: the runner-up items recorded by the reviewers (alert id fallback, UTC quiet hours, local lockout probe limits).
- **Release queue: two latent P2s recorded, deliberately NOT hand-fixed** (editing published release metadata would create a worse mismatch): (1) website release metadata points at a tag that does not exist for the published build — the version/build bump to 2.0 happened after the published tag was created, so the sync-derived tag drifts; the build-204 sync must derive from the ACTUAL tag and the deploy guard should catch a mismatch (currently it only compares buildNumber); (2) the local build helper hardcodes a Desktop path while the repo lives under Documents. Both belong to the build-204 release lane.
- **Docs queue: D2-D5 CLOSED** (index, operator notes, API surface, limitations, guides). Remaining: roadmap checkbox sync (MASTER_ROADMAP §6 still shows every V2 bullet unchecked while the log records them complete — the roadmap file itself needs the status pass).
- **Test queue:** suite count 246; drill harness has the transfer leg implementable but not yet appended (needs the UI lane's transfer wiring to exercise it end-to-end).
- **Adversarial lane is a permanent periodic lane**, not final-only — it produced the cycle's only P0 this round.

### Cycle-6 queue deltas

- **V2-G wave 3 (offline job policy): LANDED (86432ee)** — jobLifecycle expiresAt opt-in, lazy expiry on getJob/listJobs/recovery, key release without replay, TTL excluding destructive types, zero-change default. instances:job-expiry:smoke registered; job family green. I2 CLOSED.
- **V2-F wave 4 (restore targeting + preview): LANDED (21a27cc)** — read-only preview (byte-proven), same-node targetInstanceId with cross-instance restore + config rebranding, target-aware conflict refusal, safety-scope fallback. **Product bug fixed by the implementer: in-restore failures were masked as RESTORE_ROLLBACK_FAILED (rollbackRestoreFromSafetySnapshot required a path field live metadata never carried) — every real in-restore failure had skipped rollback. Now resolves from the snapshot id; covered by forced mid-mutation rollback test.** restore:targeting:smoke registered; backup family green. I3 CLOSED.
- **I4 workload transfer: DISPATCHED** (desktop orchestration: download → import → targeted restore with preview-first + confirm; extends the drill harness transfer leg). R4 reviewer dispatched over 86432ee/21a27cc.
- **Residue tripwire caught implementer development-run residue again** (both new smokes verified clean in isolation; cleaned + gate rerun).
- **V2-H/I/J survey COMPLETE** (agent_f54328a7): three bullet tables + wave plans. Sequencing decision: V2-I W1 (actor×resource×action permission-matrix harness) BEFORE V2-H exposure waves. Deferrals recorded: reverse-proxy/certs (owner decision), container trust levels (re-scope candidate), fleet update orchestration (may ride V2-G batch). NEW queue item: write docs/v2/SECURITY_TRIAGE_RECORD.md (Mimosa evidence must be in-repo for the V2-I gate) — S1 updated.
- Suite count 234 → 238 (job-expiry + restore-targeting + fleet + multi-node smokes registered).

### Cycle-5→6 queue deltas

- **R3 reviewer verdict: NOTHING ABOVE P1** on the fleet/drill wave (batch confirm gate verified service-side on the sole path; runWithConcurrency race-free; drill hermetic with real leak detection; marker fix sound). All four findings fixed + pushed (dc1fcb8): fleet-summary wording honesty, failed-batch refresh, delete-toast attempted-false wording, (ungrouped) sentinel collision (NUL-prefixed sentinel). Recorded: commit-split drift in 2748170 (message says review-fixes, content includes the wave-2 feature — mid-session git add -A sweep; unfixable without history rewrite, recorded here as the attribution of record).
- **V2-H/I/J survey COMPLETE**: three bullet tables + wave plans + drill decompositions + deferral candidates (reverse-proxy/certs → defer with owner decision; container trust levels → possibly re-scope; fleet update orchestration → may ride V2-G batch). Key campaign-artifact gap flagged: the Mimosa triage evidence must be recorded in-repo (S1 now includes "write docs/v2/SECURITY_TRIAGE_RECORD.md"). Waves queued in the implementation queue after I2/I3: V2-I W1 (actor×resource×action matrix harness) first — it hardens everything before V2-H's exposure work.
- **Cycle 6 in flight:** I2 offline job policy (jobLifecycle expiresAt), I3 restore targeting + preview (backupService), R3 findings fixed (dc1fcb8).
- **Sequencing note:** V2-I W1 (permission matrix harness) prioritized ahead of V2-H waves — hardening before exposure, per the roadmap's own ordering ("public exposure also requires V2-I security gates").

### Cycle-5 queue deltas

- **I1 fleet aggregation + batch: LANDED (bbf4d90)** — fleetService, batch start/stop with max-3 concurrency + per-node results + destructive confirm, fleet strip UI. Review R3 dispatched.
- **T2 drill harness: LANDED (bbf4d90)** — multi-node:fleet:smoke (two spawned agents, interrupt/isolation/revocation/JOB_INTERRUPTED-recovery legs; transfer/per-OS legs documented for later). Found a real product bug: stale removal markers silently deleted re-paired nodes (fixed in nodeService, regression-covered by node:local-removal smoke).
- **Implementation queue:** I2 (offline job policy) and I3 (restore targeting) now READY; I4 still blocked on I3; I7 (V2-H/I/J surveys) dispatches next cycle.
- **Review queue:** R3 (bbf4d90) dispatched (agent running); R2's fix-commit 1b04536 covered by R3's scope extension.
- **Test queue:** T2 partial → drill live (transfer/per-OS legs appended when I3/I5 land). Suite count 232 → 234.
- **Reliability:** the harness caught a REAL reliability bug pre-land: stale removal markers deleting re-paired nodes (fixed, regression smoke node:local-removal still green).
- **Clean tree at bbf4d90**; full gate + R3 reviewer dispatched at close.

---

## 1. IMPLEMENTATION QUEUE

| # | Item | Milestone | Size | Dependencies | Status |
| --- | --- | --- | --- | --- | --- |
| I1 | V2-G wave 2: fleet aggregation (per-node health/job roll-up) + cross-node batch actions with per-node results and bounded concurrency | V2-G | M-L | wave 1 ✓ (landed 91f31ef) | **READY** |
| I2 | V2-G wave 3: offline job policy — expiresAt/renewed-approval in jobLifecycle; enqueued-but-untransferred jobs expire, never replay | V2-G | S-M | wave 1 ✓ | **READY** |
| I3 | V2-F wave 4: restore targeting + preview (targetInstanceId, dry-run, conflict=refuse unless confirm) | V2-F | M-L | waves 1+2 ✓ | **READY** |
| I4 | V2-G wave 4: workload transfer via backup → import → restore to another node | V2-G | M | I1 (per-node scoping), I3 (target restore) | BLOCKED on I3 |
| I5 | V2-G wave 5 + V2-F wave 5: per-OS agent upgrade paths; remote/encrypted backup destinations | V2-G/F | L | waves 2-4 | QUEUED |
| I6 | V2-E remainder (optional): roster editing (whitelist/ops) — only if the acceptance drill needs it | V2-E | S | player rosters ✓ | PARKED (view-only satisfies bullet; revisit at drill) |
| I7 | V2-H/I/J scoping surveys | V2-H/I/J | — | V2-E/F/G substantially landed | QUEUED (dispatch when V2-G waves 1-3 land) |

## 2. REVIEW QUEUE

| # | Item | Implementer | Reviewer | Status |
| --- | --- | --- | --- | --- |
| R1 | Cycle-4 combined commit 91f31ef: backup consistency option + node lifecycle (revocation credential model, async deleteNode, manualDisconnect health gating, renderer regions) | agent ×2 | NEW reviewer required | **READY** |
| R2 | Cycle-3 workstreams (f59e608/28f66e4/1eb7f88) | agents ×3 | reviewer done (agent_f9e1773b); findings fixed in 1b04536; fix-commit itself not yet independently reviewed | PARTIAL — fold R1 reviewer over 1b04536 too |

## 3. REGRESSION QUEUE

| # | Finding | Source | Evidence | Status |
| --- | --- | --- | --- | --- |
| X1 | Residue leaks into repo root (two caught by tripwire; fixed 760e493 + smoke pins) | tripwire | instances/jobs residue | FIXED; tripwire guards recurrence |
| X2 | Hollow-green smoke exit (transaction smoke steps 5-8 never ran) | reviewer execution proof | fixed in c97cae4 + gate guard | FIXED |
| — | No open regressions | | | verified at cycle-4 close (all families green) |

## 4. TEST QUEUE

| # | Item | Type | Status |
| --- | --- | --- | --- |
| T1 | rc:validate full gate after every cycle | gate | CONTINUOUS (232→235 suites as smokes land) |
| T2 | One-machine two-node drill harness (scripts/multi-node-fleet-smoke.js per V2-G survey decomposition: enroll both, interrupt, restart desktop, per-node isolation, revoke 410, transfer, per-OS legs) | integration/recovery | **READY — build with I1/I2** |
| T3 | Failure-injection: interrupted backup mid-write (covered by interrupted-recovery smoke ✓), corrupt store mid-tick (covered ✓), restore conflict paths (needs I3) | failure-injection | PARTIAL |
| T4 | Restart/recovery: desktop restart mid-install (transaction smoke ✓), mid-restart-schedule (covered ✓), mid-restore (rollback covered ✓) | restart/recovery | MOSTLY COVERED |

## 5. HARNESS QUEUE

| # | Item | Source | Status |
| --- | --- | --- | --- |
| H1 | ssh-session-timeout margins (25ms race vs 60/110ms waits) — widen or fake clock | harness audit | PARKED (passing smoke; churn risk) |
| H2 | Source-pin smokes re-tier (50 vacuous-ish; keep as cheap regression tripwires but not blocking) | harness audit | PARKED (ordering churn; tripwire value real) |
| H3 | bootstrap:auth-order vm-extraction rework | harness audit | PARKED (whitespace-normalized pins work) |
| H4 | device-architecture 12s cap + Windows rm retries robustness | harness audit | PARKED |
| H5 | website:smoke chain split into 4 explicit suites | harness audit | PARKED (cosmetic) |
| H6 | dependency-smoke agent-token write to user config on fresh machines | pin-guard implementer risk note | **READY (S)** — rides the next harness pass |
| H7 | listBackups read-path legacy-schema write tolerance (read-only API writing on read) | reviewer P2-5 | PARKED (caught + reported via scheduleStoreErrorCode today) |

## 6. SECURITY QUEUE

| # | Item | Source | Status |
| --- | --- | --- | --- |
| S1 | Mimosa scan: ZERO P0/P1 verdict standing; re-scan after campaign code volume (841 → expect drift) | security triage | **COMPLETE at `ff9d10a`; verdict NOT VERIFIED** — the sealed deep scan (1012 findings, 255 packages) has **0 investigated**, so no clean verdict may be claimed (cycle-25 delta) |
| S2 | playitService -Command interpolation hardening (4 sites) | triage P2 | PARKED (scanner blocks candidate; non-exploitable FP path; sc.exe migration disproportionate) |
| S3 | Client-supplied runtime-pin identity is a soft bypass (dependencies:write holders can claim another workload's id) — document in operator docs + API contract | reviewer P2-3 | **READY (S)** — rides docs queue |
| S4 | Renderer sinks: verified textContent-only in all new UI (player rosters, app cards, node groups) — keep verifying each wave | triage + reviewers | CONTINUOUS ✓ |
| S5 | js-yaml DoS cap + vm removal + SHA-512 preference landed | triage P2 | DONE (6d2e7e5, eb13b83) |

## 7. RELIABILITY QUEUE

| # | Item | Source | Status |
| --- | --- | --- | --- |
| Y1 | Restart-scheduler tick-vs-CRUD lost-update (re-read + merge before final write) | reviewer P2-4 | CLOSED (cycle 10: scheduler tick re-reads and merges concurrent CRUD instead of overwriting) |
| Y2 | Scheduled restart vs in-flight durable job on the target (check open instance jobs before restarting) | reviewer P2-5 | CLOSED (cycle 10: due restart skips with `INSTANCE_BUSY`, fails closed with `JOB_QUERY_FAILED`; `restartScheduleService.js`) |
| Y3 | At-least-once restart after agent crash between execution and store write (benign double-restart) — accepted, documented | reviewer observation | ACCEPTED |
| Y4 | Verify-then-extract TOCTOU on archive digest (hash during extract instead) | reviewer P2-7 | PARKED (local trust domain) |

## 8. ROADMAP GAP QUEUE

| # | Gap | Evidence | Acceptance criteria | Status |
| --- | --- | --- | --- | --- |
| G1 | V2-D: install plan preview has no renderer UI (API-only; getTemplateInstallPlanPreview + getInstallDiskPreflight done, unreviewed functionally) | reviewer P2-8 | plan visible before install (renderer) | CLOSED — read-only plan dialog for curated templates shipped cycle 11 (`app.js` install-plan renderer); provider packs have no plan preview by design |
| G2 | V2-D: publisher-trust warning for third-party executable content | roadmap bullet | trust warning shown for non-catalog sources | CLOSED — cycle 18 renderer notice built from the policy's own exported copy (`publisher-trust:display:smoke`); honest limits (no production path reaches `verified`; `hash-mismatch` is advisory) recorded in `docs/KNOWN_LIMITATIONS.md` |
| G3 | V2-C: image update rollback (rollbackSupported:false) — build or roadmap-scope the deferral | roadmap bullet | controlled rollback where data compat allows | **DECISION NEEDED (owner)** — recommend deferral note |
| G4 | V2-D: catalog export/import + offline installation limits documented | roadmap bullet | export/import + documented limits | CLOSED — cycle 16 service (`catalogTransferService.js`), cycle 17 IPC + preload (`catalog-transfer-ipc-exposure:smoke`), cycle 25 UI; import validates and reports without persisting |
| G5 | V2-A/B: responsive/browser workflows beyond the agent-served read-only page | roadmap bullets | accessible browser workflows | DEFERRED (browser surface Option A shipped; full parity = later) |
| G6 | Docker engine live acceptance (V2-C gate) | roadmap gate | single+multi container app, bad image, occupied port, missing volume, failed update | PARTIAL — executed PASSED in cycle 25 against the real socket (`docker:smoke`); the wider destructive list was not run |
| G7 | Two-node live drill (V2-A/V2-G gate) | roadmap gate | interrupted connection, restart, revocation effective | CLOSED — executed PASSED in cycle 25 (`multi-node:fleet:smoke` spawns two real Agents; seven legs); transfer and per-OS legs stay recorded as not live |
| G8 | Operator walkthrough (V2-B gate) + stale-status agreement live check | roadmap gate | install→open→config→recover via UI | PARTIAL — `qa:acceptance` ran clean in cycle 25 (11 checks, 0 failures: launch, QA mode, owner gate, all eight pages); config-write and recovery paths not covered |
| G9 | Clean-host Debian 12 install (V2-D gate + V2-A Linux evidence) | roadmap gate | reproducible install + support-matrix sign-off | **EXTERNALLY BLOCKED** — no clean Debian host present; unblock requires hardware the owner must provide |

## 9. DOCS / STATUS QUEUE

| # | Item | Status |
| --- | --- | --- |
| D1 | Campaign log heartbeat per cycle | CONTINUOUS ✓ |
| D2 | DOCUMENTATION_INDEX: add docs/v2/ records (V2_WAVE1/2 records, campaign log, queues) | **READY (S)** |
| D3 | Runtime-pins operator note + S3 soft-bypass documentation | **READY (S)** — rides next docs commit |
| D4 | New-user guide / operator docs updates for: compose policy grants, node groups/disconnect/revoke, backup consistency + retention verdicts | **READY (M)** — after V2-G wave 2 lands |
| D5 | API/IPC contract docs: runtime-pins + restart-schedules + node lifecycle endpoints | **READY (M)** |

## 10. RELEASE QUEUE

| # | Item | Status |
| --- | --- | --- |
| RL1 | No release actions this campaign: next release = v2.0-build204, gated on ADP RC workflow + 🚀 publish | STANDING |
| RL2 | Build 199 + V1 history immutable | STANDING ✓ (untouched) |
| RL3 | Release-readiness chain deferred until campaign code waves complete (changelog → version bump → RC → sandbox acceptance → gates) | QUEUED |
| RL4 | Release tooling: promote workflow unproven end-to-end (dc6cd56 fix); exercise on next RC | QUEUED |

## 11. CLEANUP QUEUE

| # | Item | Status |
| --- | --- | --- |
| C1 | TODO/FIXME sweep across V2-touched files | **READY — at campaign end** |
| C2 | Temporary logging/debug hooks audit (dev-*.js files removed as created ✓) | CONTINUOUS ✓ |
| C3 | Dead code from superseded implementations (watch: chooseBackupType old callers) | **READY — with R1 reviewer** |
| C4 | package.json script ordering consolidation (cosmetic) | PARKED |

## 12. FINAL AUDIT QUEUE

Opens only when queues 1-8 are empty or explicitly deferred. Plan: independent multi-agent audit — roadmap reconstruction from repo vs campaign log, TODO/skipped/flaky sweep, lifecycle/shutdown/migration edge cases, security re-scan (S1), release-readiness chain, git state verification, protected-history check.

---

## AGENT POOL STATE (cycle 4 close)

- Completed: player mgmt, backup waves 1+2, pin guard, consistency, node lifecycle (5 implementers) + reviewers (2) + auditors (3) + security triage (1) this campaign.
- Running: none (all integrated).
- Next dispatch: R1 reviewer; I1/I2 implementers; T2 drill harness.

### Cycle-9 queue deltas

- **V2-G wave 5 (Linux agent self-update + capability report): LANDED (98e15f7)** — systemd-run transient swap with staging temp+rename, backup-before-swap + rollback, pure-sh post-exit script with observable result markers; agentUpdate capability report; honest Windows parity (desktop-driven task lifecycle, never in-place swap). agent:self-update:smoke registered. Wave 5 partial: remote-agent push upgrades remain future (needs a runtime-download authority).
- **V2-H wave 1 (network inventory): LANDED (fe4ddb2)** — interfaces/listeners/conflicts/checkPort, agent REST route (system:read), full desktop chain, matrix rows. network-inventory:smoke registered.
- **R7 verdict: no P0, 3×P1 — ALL FIXED (396ae08):**
  1. swap-script rollback masking (`|| true` on the restore mv made the orchestrator claim success with neither runtime in place) → new failed-rollback-failed marker, rolledBack reported honestly.
  2. failed Linux update left the agent stopped → catch path best-effort restarts on the previous runtime, restartAttempted/agentRestarted reported.
  3. Windows netstat parser locale failure (non-English state words yielded silently empty TCP inventories) → state-agnostic listener selection via the foreign wildcard-port-0 shape + parse telemetry surfacing shape failures.
- **P2 ledger added:** capability honesty for unitless Linux hosts; self-copy staging note (live drill must validate genuine version-delta swap); UDP wildcard+specific false positives; alias-teeth scope; preview getStatus self-heal caveat.
- Suite count 240 → 242. Tree clean at 396ae08; gate rerunning at close.
